---
import { Image } from 'astro:assets';
import type { ImageMetadata } from 'astro';

interface Props {
  logo: ImageMetadata;
  title: string;
  org: string;
  year: string;
}

const { logo, title, org, year } = Astro.props;
---

<div class="education-item">
  <Image src={logo} alt="" width={264} height={264} quality={90} fit="contain" class="logo" />
  <div class="description">
    <p class="text-label" data-i18n-scope>{title}</p>
    <p class="text-body org">{org}</p>
    <p class="text-body year">{year}</p>
  </div>
</div>

<style>
  .education-item {
    display: flex;
    align-items: flex-start;
    gap: 28px;
  }

  .logo {
    width: 88px;
    height: 88px;
    border-radius: var(--radius-lg);
    object-fit: cover;
    flex-shrink: 0;
  }

  .description {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    justify-content: center;
    gap: 8px;
  }

  .org,
  .year {
    color: var(--color-zinc-dust);
  }
</style>
